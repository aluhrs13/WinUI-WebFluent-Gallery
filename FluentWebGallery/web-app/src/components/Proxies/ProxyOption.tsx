import { Fragment, Component, render } from "preact";
import { Option } from "@fluentui/react-components";

export class ProxyOption extends Component {
  constructor() {
    super();
  }

  render() {
    return (
      <Fragment>
        <Option />
      </Fragment>
    );
  }
}
