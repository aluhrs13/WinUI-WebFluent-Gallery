import { Fragment, Component, render } from "preact";
import { Image } from "@fluentui/react-components";

export class ProxyIcon extends Component {
  constructor() {
    super();
  }

  render() {
    return (
      <Fragment>
        <Image />
      </Fragment>
    );
  }
}
